#' @section Covariates methods:
#'
#' \describe{
#'   \item{\code{read_eth_files(..., cov_path=NULL)}}{Read the ETH format files together with the covariates file (by default one covariate for all observations).}
#'   \item{\code{read_plink_files(..., cov_path=NULL)}}{Read the PLINK format files together with the covariates file (by default one covariate for all observations).}
#'   \item{\code{update_covariates_file(cov_path)}}{Update covariates file.}
#'
#'   \item{\code{write_eth_files(..., cov_path=NULL)}}{Write the ETH format files together with the covariates file (by default do not write covariates).}
#' }
